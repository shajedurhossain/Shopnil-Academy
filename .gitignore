# Automation logs (scripts themselves are version-controlled)
.automation/watcher.log

# OS / editor noise
.DS_Store
Thumbs.db
desktop.ini
*.bak
*.tmp
*~

# ── Credentials — NEVER commit these ──
gcp-key.json
.env
*.key
*-key.json
service-account*.json

# ── Media — served from Cloudflare R2, not GitHub Pages ──
/audio/
/video/
audio_es/
audio_de/
audio_bn/
*.mp3
*.mp4
*.wav

# ── Python ──
__pycache__/
*.pyc
venv/
.venv/

# ── Node ──
node_modules/
